apiVersion: v1
kind: Secret
metadata:
  name: db-app
  namespace: newsletter
type: kubernetes.io/basic-auth
# Placeholder only for bootstrap/demo environments.
# Production: source this secret from a secret manager (for example: HashiCorp Vault via External Secrets Operator),
# instead of storing credentials in Git.
stringData:
  username: app
  password: change-me
