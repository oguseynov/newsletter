apiVersion: v1
kind: Secret
metadata:
  name: clickstack-otel
  namespace: newsletter
type: Opaque
# Placeholder values for bootstrap/demo environments.
# Secret-manager backed sourcing will be implemented later (for example: HashiCorp Vault via External Secrets Operator).
# For now, keep only placeholder values in Git and set real values out of band.
stringData:
  endpoint: ingest.<your-clickstack-domain>:4317
  api-key: <your-clickstack-ingestion-api-key>
